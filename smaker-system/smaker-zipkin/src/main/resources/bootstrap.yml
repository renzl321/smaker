server:
  port: 5002

spring:
  application:
    name: smaker-zipkin
  profiles:
    active: dev
  #配置中心
  cloud:
     nacos:
         discovery:
            server-addr: smakercloud-nacos:8848
         config:
              file-extension: yml
              server-addr: smakercloud-nacos:8848


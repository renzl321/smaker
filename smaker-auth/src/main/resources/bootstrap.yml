server:
  port: 3000

spring:
  application:
    name: smaker-auth
  profiles:
    active: dev
  # 配置中心
  cloud:
    nacos:
      discovery:
        server-addr: smakercloud-nacos:8848
      config:
        file-extension: yml
        server-addr: smakercloud-nacos:8848


